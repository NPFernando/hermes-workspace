name: CodeQL

on:
  push:
    branches: [main, production]
  pull_request:
    branches: [main, production]
  schedule:
    - cron: '31 3 * * 1'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze JavaScript and TypeScript
    runs-on: ubuntu-latest
    # Fail closed if the hosted analyzer stalls. Deployment gates require a
    # successful exact-HEAD result, so an infinite run must become an explicit
    # terminal failure that can be retried or investigated.
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: /language:javascript-typescript
